Requires simplejson, oauth2 (can be installed via easy_install or pip)

Create an application at dev.twitter.com and use the OAuth values from there.
Rename config_clean.py to config.py after inserting values.

OAuth/Twitter library from http://code.google.com/p/oauth-python-twitter2/